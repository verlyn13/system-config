[tools]
node = "24.9.0"
python = "3.13.7"

[tasks.build]
run = "echo build && exit 0"
description = "Build (placeholder)"

[tasks.test]
run = "echo test && exit 0"
description = "Test (placeholder)"

[tasks.lint]
run = "echo lint && exit 0"
description = "Lint (placeholder)"

[tasks.obs:run]
run = "../../scripts/obs-run.sh --project ${PROJECT_ID}"
description = "Run observers for this project"

